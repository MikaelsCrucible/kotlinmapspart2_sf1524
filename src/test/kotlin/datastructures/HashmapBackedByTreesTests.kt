package datastructures
/*
import kotlin.test.Test
import kotlin.test.assertNull

class HashmapBackedByTreesTests : CustomMutableMapTestsParent() {
    override fun emptyCustomMutableMapStringInt(): CustomMutableMap<String, Int> = HashmapBackedByTrees(String::compareTo)

    override fun emptyCustomMutableMapCollidingStringInt(): CustomMutableMap<CollidingString, Int> = HashmapBackedByTrees(CollidingString::compareTo)

    @Test
    fun testPlaceholder() {
        // A placeholder so that this class has at least one test, because sometimes IDEs
        // behave strangely if all tests are in the superclass.
        assertNull(emptyCustomMutableMapStringInt()["a"])
    }
}
*/
