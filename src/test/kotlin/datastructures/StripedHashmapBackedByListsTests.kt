package datastructures
/*
import kotlin.test.Test
import kotlin.test.assertNull

class StripedHashmapBackedByListsTests : ThreadSafeCustomMutableMapTestsParent() {
    override fun emptyThreadSafeMapIntString(): CustomMutableMap<Int, String> = StripedHashmapBackedByLists()

    override fun emptyCustomMutableMapStringInt(): CustomMutableMap<String, Int> = StripedHashmapBackedByLists()

    override fun emptyCustomMutableMapCollidingStringInt(): CustomMutableMap<CollidingString, Int> = StripedHashmapBackedByLists()

    @Test
    fun testPlaceholder() {
        // A placeholder so that this class has at least one test, because sometimes IDEs
        // behave strangely if all tests are in the superclass.
        assertNull(emptyThreadSafeMapIntString()[0])
    }
}
*/
