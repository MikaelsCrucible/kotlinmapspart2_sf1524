package datastructures
/*
import kotlin.test.Test
import kotlin.test.assertNull

class LockedListBasedMapTests : ThreadSafeCustomMutableMapTestsParent() {
    override fun emptyThreadSafeMapIntString(): CustomMutableMap<Int, String> = LockedListBasedMap()

    override fun emptyCustomMutableMapStringInt(): CustomMutableMap<String, Int> = LockedListBasedMap()

    override fun emptyCustomMutableMapCollidingStringInt(): CustomMutableMap<CollidingString, Int> = LockedListBasedMap()

    @Test
    fun testPlaceholder() {
        // A placeholder so that this class has at least one test, because sometimes IDEs
        // behave strangely if all tests are in the superclass.
        assertNull(emptyThreadSafeMapIntString()[0])
    }
}
*/
