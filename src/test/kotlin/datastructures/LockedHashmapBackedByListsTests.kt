package datastructures
/*
import kotlin.test.Test
import kotlin.test.assertNull

class LockedHashmapBackedByListsTests : ThreadSafeCustomMutableMapTestsParent() {
    override fun emptyThreadSafeMapIntString(): CustomMutableMap<Int, String> = LockedHashmapBackedByLists()

    override fun emptyCustomMutableMapStringInt(): CustomMutableMap<String, Int> = LockedHashmapBackedByLists()

    override fun emptyCustomMutableMapCollidingStringInt(): CustomMutableMap<CollidingString, Int> = LockedHashmapBackedByLists()

    @Test
    fun testPlaceholder() {
        // A placeholder so that this class has at least one test, because sometimes IDEs
        // behave strangely if all tests are in the superclass.
        assertNull(emptyThreadSafeMapIntString()[0])
    }
}
*/
